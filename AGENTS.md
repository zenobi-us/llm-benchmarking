## Agent skills

### Issue tracker

GitHub Issues via `gh`; pull requests are not a request surface. See `docs/agents/issue-tracker.md` relative to the active Matt Pocock alignment root.

### Triage labels

Use `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. See `docs/agents/triage-labels.md` relative to the active Matt Pocock alignment root.

### Domain docs

Single-context project: use `CONTEXT.md` and `docs/adr/` beneath the alignment root. See `docs/agents/domain.md` relative to the active Matt Pocock alignment root.
